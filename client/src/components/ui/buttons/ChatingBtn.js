import styled from "styled-components"

const ChatingBtn = () =>{
  <Container>
    채팅하기
  </Container>
}
export default ChatingBtn

const Container = styled.div`
border-radius: 50%;
background-color:  ${(props) => props.theme.primary};



`